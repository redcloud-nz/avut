# Release notes

One file per release: `v{version}.md`, matching `nz.avut.version` in
`package.json`. It's the hand-written top of the GitHub Release —
[`manage-release-version.yml`](../../.github/workflows/manage-release-version.yml)
concatenates it with a categorised PR list and passes the result as
`body_path`. That list is generated by
[`generate-release-notes.sh`](../../.github/scripts/generate-release-notes.sh),
not GitHub's built-in release-notes generator — the built-in one only finds
PRs based on `production`, which in this repo's model is just the release PR
itself. The script instead resolves the actual feature PRs that shipped,
regardless of which branch they targeted (see [`releasing.md`](../releasing.md#release-notes)).

**The file must exist before `production` is pushed** — the workflow fails the
release if `docs/releases/v{version}.md` is missing or empty. Add it on the
release branch, in the one PR that ships the release (step 1 of
[`releasing.md`](../releasing.md)); the `/avut-release` skill drafts it for you.

## Format

Keep it short — a sentence or two of framing, then the highlights a reader
cares about. The exhaustive commit/PR list is appended automatically, so don't
reproduce it.

```markdown
## v0.9 (Laburnum)

<One or two sentences: the theme of this release.>

### Highlights

- **<Thing>** — what changed and why it matters.
- **<Thing>** — …

### Upgrade notes

<Anything an operator must do — migrations, env vars, config. Omit the section
if there's nothing.>
```

`### Upgrade notes` earns its place only when there's an action to take.
