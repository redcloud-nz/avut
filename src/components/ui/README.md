# UI Component Catalogue

Quick reference for picking the right primitive in `src/components/ui/` without opening every file. Descriptions are terse by design — check the file itself for props/usage details.

## Radix/shadcn primitives

| File                  | Exports                                                                                                                                                                    | Purpose                                                                                                                                                                       |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `accordion.tsx`       | `Accordion`, `AccordionItem`, `AccordionTrigger`, `AccordionContent`                                                                                                       | Radix Accordion wrapper — collapsible expand/collapse panels.                                                                                                                 |
| `alert-dialog.tsx`    | `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogAction`, `AlertDialogCancel`, etc.                        | Radix AlertDialog wrapper — blocking confirm/destructive-action modal.                                                                                                        |
| `avatar.tsx`          | `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarGroup`, `AvatarGroupCount`, `AvatarBadge`                                                                                | Radix Avatar wrapper — user/entity image with fallback, plus grouping/badge helpers.                                                                                          |
| `badge.tsx`           | `Badge`, `badgeVariants`                                                                                                                                                   | CVA-based small pill/tag for status or count labels.                                                                                                                          |
| `breadcrumb.tsx`      | `Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator`, `BreadcrumbEllipsis`                                          | Plain HTML breadcrumb nav primitives for page trail.                                                                                                                          |
| `button-group.tsx`    | `ButtonGroup`, `ButtonGroupText`, `ButtonGroupSeparator`                                                                                                                   | Layout primitive that visually joins adjacent buttons/inputs into one control.                                                                                                |
| `button.tsx`          | `Button`, `buttonVariants`, `MutationButton`                                                                                                                               | Core clickable button (CVA variants); `MutationButton` shows pending/success state from a tRPC/React Query mutation and stays clickable after an error so the user can retry. |
| `calendar.tsx`        | `Calendar`, `CalendarDayButton`                                                                                                                                            | Wrapper around react-day-picker for date picking.                                                                                                                             |
| `checkbox.tsx`        | `Checkbox`                                                                                                                                                                 | Radix Checkbox wrapper — single boolean toggle input.                                                                                                                         |
| `collapsible.tsx`     | `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`                                                                                                                  | Thin Radix Collapsible wrapper — generic show/hide content region.                                                                                                            |
| `command.tsx`         | `Command`, `CommandDialog`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`, `CommandShortcut`, `CommandSeparator`                            | cmdk-based command palette/searchable list; basis for `searchable-select`.                                                                                                    |
| `dialog.tsx`          | `Dialog`, `DialogTrigger`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`, `DialogCloseButton`                                        | Radix Dialog wrapper — standard modal for forms/detail views.                                                                                                                 |
| `dropdown-menu.tsx`   | `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuCheckboxItem`, `DropdownMenuRadioItem`, `DropdownMenuSub`, etc.             | Radix DropdownMenu wrapper — full menu system incl. checkbox/radio items and submenus.                                                                                        |
| `input-otp.tsx`       | `InputOTP`, `InputOTPGroup`, `InputOTPSlot`, `InputOTPSeparator`                                                                                                           | Wrapper around `input-otp` lib — one-time-passcode/verification-code entry.                                                                                                   |
| `input.tsx`           | `Input`                                                                                                                                                                    | Plain styled `<input>` primitive.                                                                                                                                             |
| `kbd.tsx`             | `Kbd`, `KbdGroup`                                                                                                                                                          | Styled `<kbd>` for a single key; `KbdGroup` lays out several for a chord.                                                                                                     |
| `label.tsx`           | `Label`                                                                                                                                                                    | Radix Label wrapper — accessible form field label.                                                                                                                            |
| `pagination.tsx`      | `Pagination`, `PaginationContent`, `PaginationItem`, `PaginationLink`, `PaginationPrevious`, `PaginationNext`, `PaginationEllipsis`                                        | Plain HTML pagination nav built on the `Button` primitive.                                                                                                                    |
| `popover.tsx`         | `Popover`, `PopoverTrigger`, `PopoverContent`, `PopoverAnchor`, `PopoverHeader`, `PopoverTitle`, `PopoverDescription`                                                      | Radix Popover wrapper — floating panel anchored to a trigger.                                                                                                                 |
| `radio-group.tsx`     | `RadioGroup`, `RadioGroupItem`                                                                                                                                             | Radix RadioGroup wrapper — single-choice radio button set.                                                                                                                    |
| `scroll-area.tsx`     | `ScrollArea`, `ScrollBar`                                                                                                                                                  | Radix ScrollArea wrapper — custom-styled scrollable container.                                                                                                                |
| `select.tsx`          | `Select`, `SelectGroup`, `SelectValue`, `SelectTrigger`, `SelectContent`, `SelectLabel`, `SelectItem`, `SelectSeparator`, `SelectScrollUpButton`, `SelectScrollDownButton` | Radix Select wrapper — standard dropdown (non-searchable; see `searchable-select` for search).                                                                                |
| `separator.tsx`       | `Separator`                                                                                                                                                                | Radix Separator wrapper — thin visual divider line.                                                                                                                           |
| `sheet.tsx`           | `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetFooter`, `SheetTitle`, `SheetDescription`                                                                    | Radix Dialog wrapper styled as a slide-in side panel (drawer).                                                                                                                |
| `sidebar.tsx`         | `Sidebar`, `SidebarProvider`, `SidebarTrigger`, `SidebarContent`, `SidebarMenu`, `SidebarMenuButton`, `SidebarGroup`, `SidebarRail`, `useSidebar`, etc.                    | Large composite app-shell sidebar system with collapse/mobile state via context.                                                                                              |
| `skeleton.tsx`        | `Skeleton`                                                                                                                                                                 | Plain animated placeholder block for loading states.                                                                                                                          |
| `slider.tsx`          | `Slider`                                                                                                                                                                   | Radix Slider wrapper — draggable numeric range input.                                                                                                                         |
| `sonner.tsx`          | `Toaster`                                                                                                                                                                  | Wrapper around `sonner` toast library, themed to app tokens; render once near app root, trigger with `toast()`.                                                               |
| `switch.tsx`          | `Switch`                                                                                                                                                                   | Radix Switch wrapper — boolean on/off toggle.                                                                                                                                 |
| `table.tsx`           | `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableHeadCell`, `TableRow`, `TableCell`, `TableCaption`                                                               | Plain HTML `<table>` styling primitives (the `Kaga` data-table system builds on these).                                                                                       |
| `tabs.tsx`            | `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`                                                                                                                           | Radix Tabs wrapper — tabbed content switching.                                                                                                                                |
| `textarea.tsx`        | `Textarea`                                                                                                                                                                 | Plain styled `<textarea>` primitive.                                                                                                                                          |
| `toggle-group.tsx`    | `ToggleGroup`, `ToggleGroupItem`                                                                                                                                           | Radix ToggleGroup wrapper — set of togglable buttons (single/multi select).                                                                                                   |
| `toggle.tsx`          | `Toggle`, `toggleVariants`                                                                                                                                                 | Radix Toggle wrapper — single pressed/unpressed button.                                                                                                                       |
| `tooltip.tsx`         | `Tooltip`, `TooltipTrigger`, `TooltipContent`, `TooltipProvider`                                                                                                           | Radix Tooltip wrapper — hover/focus text hint; app must mount one `TooltipProvider` near root.                                                                                |
| `visually-hidden.tsx` | `VisuallyHidden`                                                                                                                                                           | Direct re-export of Radix VisuallyHidden — hides content visually but keeps it for screen readers.                                                                            |

## Custom/composite components

| File                        | Exports                                                                                                                                                                                                         | Purpose                                                                                                                                                              |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `card.tsx`                  | `Card`, `CardHeader`, `CardFooter`, `CardTitle`, `CardAction`, `CardDescription`, `CardContent`, `CardLoadingFallback`                                                                                          | Container/card layout system; `CardLoadingFallback` shows a `RainbowSpinner` while content loads.                                                                    |
| `copyright.tsx`             | `CopyrightString`                                                                                                                                                                                               | Trivial text component rendering "© {year} A.V.U.T. Project."                                                                                                        |
| `description-list.tsx`      | `DL`, `DLTerm`, `DLDetails`, `DLDateDetails`                                                                                                                                                                    | `<dl>`-based key/value display list; `DLDateDetails` formats a date + relative-time pair.                                                                            |
| `editable.tsx`              | `EditableCard`                                                                                                                                                                                                  | Wraps `Card` with React context for View/Edit/Create mode + target id, for inline-editable card UIs.                                                                 |
| `empty.tsx`                 | `Empty`, `EmptyHeader`, `EmptyMedia`, `EmptyTitle`, `EmptyDescription`, `EmptyContent`                                                                                                                          | Empty-state block (icon + title + description + actions).                                                                                                            |
| `field-value.tsx`           | `FieldValue`                                                                                                                                                                                                    | Read-only "display value" cell with built-in formatters (date, datetime, id, case transforms).                                                                       |
| `field.tsx`                 | `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `FieldGroup`, `FieldLegend`, `FieldSeparator`, `FieldSet`, `FieldContent`, `FieldTitle`                                                                | react-hook-form-style form field layout (label/description/error wrapper around any input).                                                                          |
| `hotkey-kbd.tsx`            | `HotkeyKbd`                                                                                                                                                                                                     | Renders a `@tanstack/react-hotkeys` hotkey as platform-aware `Kbd`/`KbdGroup` (one `Kbd` per key).                                                                   |
| `input-group.tsx`           | `InputGroup`, `InputGroupAddon`, `InputGroupButton`, `InputGroupText`, `InputGroupInput`, `InputGroupTextarea`                                                                                                  | Wraps `Input`/`Textarea`/`Button` to add icons/buttons/text inside a bordered input container.                                                                       |
| `item.tsx`                  | `Item`, `ItemGroup`, `ItemSeparator`, `ItemMedia`, `ItemContent`, `ItemTitle`, `ItemDescription`, `ItemActions`, `ItemHeader`, `ItemFooter`                                                                     | Generic "list row" composite (icon/image + title/description + actions).                                                                                             |
| `link.tsx`                  | `EmailLink`, `PhoneLink`, `ExternalLink`, `GitHubIssueLink`                                                                                                                                                     | `<a>` wrappers for mailto/tel/external links with NZ phone formatting and GitHub issue linking.                                                                      |
| `list.tsx`                  | `List`, `ListItem`                                                                                                                                                                                              | Trivial styled `<ul>`/`<li>` for prose bullet lists.                                                                                                                 |
| `loading.tsx`               | `LoadingSpinner`, `LoadingOverlay`, `LoadingFallback`, `PageLoadingSpinner`, `RainbowSpinner`                                                                                                                   | Loading indicators — simple spinner, full-page overlay, inline fallback, animated "rainbow" spinner.                                                                 |
| `menu-action.tsx`           | `MenuAction`, `useMenuActionHotkeys`                                                                                                                                                                            | `DropdownMenuItem` for an action verb + `HotkeyKbd` badge; `useMenuActionHotkeys(actions, category)` registers the `Alt+<key>` shortcuts for the menu's action list. |
| `notification.tsx`          | `Notifications`, `NotificationsEmpty`, `Notification`, `NotificationHeader`, `NotificationContent`, `NotificationFooter`, `NotificationTitle`, `NotificationDescription`, `NotificationDate`, `UnreadIndicator` | Notification-list family, structurally mirrors the `item.tsx`/`empty.tsx` pattern.                                                                                   |
| `password-input.tsx`        | `PasswordInput`                                                                                                                                                                                                 | Built on `InputGroup` — text input with show/hide-password toggle button.                                                                                            |
| `save-status-indicator.tsx` | `SaveStatusIndicator`                                                                                                                                                                                           | Small status text/spinner reflecting a React Query mutation's status (idle/pending/success/error).                                                                   |
| `searchable-select.tsx`     | `SearchableSelect`                                                                                                                                                                                              | Combines `Popover` + `Command` — a searchable/filterable select (combobox alternative to `select.tsx`).                                                              |
| `stat-item.tsx`             | `StatItem`, `StatItemValue`, `StatItemTitle`, `StatItemDescription`                                                                                                                                             | Dashboard "stat tile" family (big number + title + description); `StatItemValue` can show a `Skeleton` while loading.                                                |
| `stepper.tsx`               | `Stepper`                                                                                                                                                                                                       | Multi-step progress nav (numbered steps with complete/current/upcoming states).                                                                                      |
| `text.tsx`                  | `Text`                                                                                                                                                                                                          | Trivial muted `<p>` text primitive.                                                                                                                                  |
| `typography.tsx`            | `Heading`, `Paragraph`, `Blockquote`, `InlineCode`, `Description`, `ObjectName`                                                                                                                                 | Prose/typography primitives for headings (h1–h6) and body text.                                                                                                      |

## Notable families

- **`field.tsx` + `field-value.tsx`** — `field.tsx` is the edit-mode form field shell (label, description, error); `field-value.tsx` is the read-only display counterpart. Pick based on whether the surrounding page is editable or view-only.
- **`item.tsx` + `empty.tsx` + `notification.tsx`** — share the same structural convention (Group/Media/Content/Title/Description/Actions/Header/Footer). `item.tsx` is the generic list-row version, `empty.tsx` is for empty states, `notification.tsx` is a notification-specific variant.
- **`input-group.tsx` + `password-input.tsx` + `command.tsx`** — `input-group.tsx` is the base composite; `password-input.tsx` and `command.tsx`'s `CommandInput` both build on top of it.
- **`searchable-select.tsx`** — built from `command.tsx` + `popover.tsx` together.
- **`sidebar.tsx`** — a large composite assembled from `sheet.tsx`, `button.tsx`, `input.tsx`, `separator.tsx`, `skeleton.tsx`, and `tooltip.tsx` — treat as its own subsystem rather than a simple primitive.
- **`card.tsx` + `loading.tsx`** — `CardLoadingFallback` in `card.tsx` consumes `RainbowSpinner` from `loading.tsx`.
- **`toggle.tsx` + `toggle-group.tsx`** — `toggle-group.tsx` reuses `toggleVariants` exported from `toggle.tsx` for consistent styling.
- **`kbd.tsx` + `hotkey-kbd.tsx` + `menu-action.tsx`** — `kbd.tsx` is the raw `Kbd`/`KbdGroup`; `hotkey-kbd.tsx` turns a hotkey string into them; `menu-action.tsx` renders a menu item + its `HotkeyKbd` badge (keys sourced from `@/lib/hotkeys`).
  </content>
