/*
 *  Copyright (c) 2026 A.V.U.T. Project.
 *  Licensed under the MIT License. See LICENSE.md in the project root for license information.
 *
 * Paths: /orgs/[slug]/admin/teams/[team_id]
 *
 * Empty on purpose — the detail content is rendered by `../layout.tsx`, which wraps this
 * as `children`. This file exists only to match the bare team-detail path within the
 * `(detail)` route group.
 */

export default function TeamDetail_Page() {
    return null;
}
